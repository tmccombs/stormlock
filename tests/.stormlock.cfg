[default]
ttl=1 days
principal=test_user

[psqltest]
backend=postgresql

[psqltest2]
backend=postgresql

[ddbtest]
backend=dynamodb

[ddbtest2]
backend=dynamodb

[redistest]
backend=redis

[redistest2]
backend=redis

[etcdtest]
backend=etcd

[etcdtest2]
backend=etcd

[backend.dynamodb]
#table=stormlock
endpoint=http://localhost:8000
access_key_id=test
secret_access_key=test
#profile=test
region=us-east-1

[backend.postgresql]
connection = dbname=test
# table = stormlock

# vim: ft=dosini
